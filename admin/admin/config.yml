backend:
  name: git-gateway          # NetlifyのGit Gateway経由で認証
  branch: main               # 更新するブランチ名

media_folder: "images"       # 写真のアップロード先フォルダ
public_folder: "/images"     # HTML内での写真参照パス

# ────────────── コレクション定義 ──────────────
collections:

  # ① ニュース・記事
  - name: "news"
    label: "ニュース・記事"
    folder: "content/news"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "タイトル",     name: "title",    widget: "string" }
      - { label: "日付",         name: "date",     widget: "datetime" }
      - { label: "カテゴリ",     name: "category", widget: "select",
          options: ["展示会", "マルシェ", "メディア掲載", "連携・協定", "お知らせ"] }
      - { label: "サムネイル",   name: "image",    widget: "image", required: false }
      - { label: "本文",         name: "body",     widget: "markdown" }
      - { label: "公開",         name: "published", widget: "boolean", default: true }

  # ② メンバー
  - name: "members"
    label: "メンバー"
    folder: "content/members"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "氏名",         name: "title",  widget: "string" }
      - { label: "役職・肩書き", name: "role",   widget: "string" }
      - { label: "専門・工芸",   name: "craft",  widget: "string" }
      - { label: "プロフィール写真", name: "image", widget: "image" }
      - { label: "紹介文",       name: "bio",    widget: "text", required: false }
      - { label: "表示順",       name: "order",  widget: "number", default: 99 }
      - { label: "掲載中",       name: "active", widget: "boolean", default: true }

  # ③ 活動・実績
  - name: "activities"
    label: "活動・実績"
    folder: "content/activities"
    create: true
    slug: "{{year}}-{{slug}}"
    fields:
      - { label: "タイトル",   name: "title",    widget: "string" }
      - { label: "時期",       name: "date",     widget: "string",
          hint: "例：2025.03" }
      - { label: "種別",       name: "type",     widget: "select",
          options: ["展示会", "イベント", "連携", "メディア掲載"] }
      - { label: "サムネイル", name: "image",    widget: "image", required: false }
      - { label: "説明文",     name: "body",     widget: "text" }
      - { label: "注目記事",   name: "featured", widget: "boolean", default: false }
